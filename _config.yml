title: "Cloud & Development Services, Empowering businesses through AI/Machine Learnting"
email: "tech@cloudcontinuous.com"
description: >- # this means to ignore newlines until "baseurl:"
 Cloud engineering & Development strategies & Consulting Services. Increase your business value distribution by utilizing cloud and development services. 
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://cloudcontinuous.com" # the base hostname & protocol for your site, e.g. http://example.com or  https://tradejk.netlify.com
facebook_url: ""
twitter_url: ""
github_url:  ""
pinterest_url: ""
linkedin_url: "https://www.linkedin.com/company/cloudcontinuous"
instagram_url: "https://www.instagram.com/cloudcontinuous"
youtube_url: ""
logo: "/assets/images/cc-logo-white-250-63.png"
logo_2: "/assets/images/cc-logo-blue-400-100.png"
mailchimp_list: ""

permalink: "/:title"

copyright_text: "Copyright ©2024. All Rights Reserved to CloudContinuous LLC"
phone_number: "+1 (312)-248-7619"
cover_image: "/assets/images/page-title-bg.jpg"


# Build settings
markdown: kramdown
theme: minima
plugins:
  - jekyll-feed
  - jekyll-paginate-v2
  - jekyll-archives
  - jekyll-coffeescript
  - jekyll-seo-tag
  - jekyll-sitemap
sass:
    sass_dir: assets/scss
    style: compressed

########################################################
# Site configuration for the Jekyll 3 Pagination Gem
# The values here represent the defaults if nothing is set
pagination:
  
  # Site-wide kill switch, disabled here it doesn't run at all 
  enabled: true

  # Set to 'true' to enable pagination debugging. This can be enabled in the site config or only for individual pagination page
  debug: true

  # The default document collection to paginate if nothing is specified ('posts' is default)
  collection: 'posts'

  # How many objects per paginated page, used to be `paginate` (default: 0, means all)
  per_page: 6

  # The permalink structure for the paginated pages (this can be any level deep)
  #permalink: '/page/:num/' # Pages are index.html inside this folder (default)
  permalink: '/:num/' # Pages are simple html file
  #permalink: '/page/:num' # Pages are html files, linked jekyll extensionless permalink style.

  # Optional the title format for the paginated pages (supports :title for original page title, :num for pagination page number, :max for total number of pages)
  title: ':title - page :num'

  # Limit how many pagenated pages to create (default: 0, means all)
  limit: 0
  
  # Optional, defines the field that the posts should be sorted on (omit to default to 'date')
  sort_field: 'date'

  # Optional, sorts the posts in reverse order (omit to default decending or sort_reverse: true)
  sort_reverse: true

  # Optional, the default category to use, omit or just leave this as 'posts' to get a backwards-compatible behavior (all posts)
  category: 'posts'

  # Optional, the default tag to use, omit to disable
  tag: 'Software Agency'

  # Optional, the default locale to use, omit to disable (depends on a field 'locale' to be specified in the posts, 
  # in reality this can be any value, suggested are the Microsoft locale-codes (e.g. en_US, en_GB) or simply the ISO-639 language code )
  locale: '' 

 # Optional,omit or set both before and after to zero to disable. 
 # Controls how the pagination trail for the paginated pages look like. 
  trail: 
    before: 2
    after: 2

  # Optional, the default file extension for generated pages (e.g html, json, xml).
  # Internally this is set to html by default
  extension: 'html'

  # Optional, the default name of the index file for generated pages (e.g. 'index.html')
  # Without file extension
  indexpage: 'index'

tag_page_layout: tag_page
tag_page_dir: tag
tag_permalink_style: pretty

jekyll-archives:
  enabled: all
  layout: category
  permalinks:
    category: '/category/:name/'

feed:
  collections:
    posts:
      path: "/index.xml"

# autopages:

#   # Site-wide kill switch, disable here and it doesn't run at all 
#   enabled: true

#   # Category pages, omit entire config element to disable
#   categories: 
#     # Optional, the list of layouts that should be processed for every category found in the site
#     layouts: 
#       - 'autopage_category.html'
#     # Optional, the title that each category paginate page should get (:cat is replaced by the Category name)
#     title: 'Posts in category :cat'
#     # Optional, the permalink for the  pagination page (:cat is replaced), 
#     # the pagination permalink path is then appended to this permalink structure
#     permalink: '/category/:cat'
#     # Optional, when true logging related to category pages will be supressed.
#     silent: false
#     slugify:
#       mode: 'default'   # :cat is slugified. Modes: default, raw, pretty, ascii, latin
#       case: false       # Whether to replace all uppercase letters with their lowercase counterparts

#   # Collection pages, omit to disable
#   collections:
#     layouts: 
#       - 'default_2.html'
#     title: 'Posts in collection :coll' # :coll is replaced by the collection name
#     permalink: '/case-studies/:coll'
#     silent: false
#     slugify:
#       mode: 'default'   # :coll is slugified.
#       case: false 
  
#   # Tag pages, omit to disable
#   tags:
#     layouts: 
#       - 'autopage_tags.html'
#     title: 'Posts tagged with :tag' # :tag is replaced by the tag name
#     permalink: '/tag/:tag'
#     silent: false
#     slugify:
#       mode: 'default'   # :tag is slugified.
#       case: false